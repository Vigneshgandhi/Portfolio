# Portfolio

Personal portfolio website built with React, Tailwind CSS, Framer Motion, and React Router.

## Scripts

```bash
npm start
npm run build
npm test
```

## Development

Run `npm start` and open `http://localhost:3000` to view the app locally.

Run `npm run build` to create the production build in the `build` folder.
